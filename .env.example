APP_ENV=local
LOG_LEVEL=INFO
DATABASE_URL=sqlite+aiosqlite:///./dietologist-demo.db
API_ADMIN_TOKEN="" # Configure locally when needed.
TELEGRAM_BOT_TOKEN="" # Configure locally when needed.
TELEGRAM_ADMIN_ID="" # Configure locally when needed.
TELEGRAM_ALLOWED_IDS="" # Configure locally when needed.
OPENAI_API_KEY="" # Configure locally when needed.
OPENAI_MODEL=gpt-5.5
HEALTH_SYNC_TOKEN="" # Configure locally when needed.
DEFAULT_TIMEZONE=UTC
